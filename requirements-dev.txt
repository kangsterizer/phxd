# Dev / test dependencies. Keep minimal — heavy fixtures aren't needed
# for the binary protocol layer, and we want CI runtime measured in
# seconds. Pin majors to keep behaviour stable but allow patch bumps.
-r requirements.txt
pytest>=8,<9
pytest-cov>=5,<6
# pytest-xdist runs tests across multiple workers in parallel. The unit
# suite is small enough today that single-process is faster (worker
# startup ≈ 0.5s, the whole suite finishes in 30ms), but as we add
# integration tests against the text DB and Twisted plumbing, parallel
# execution will start paying off. The pytest.ini ``-n auto`` flag
# picks worker count = CPU count by default; override with ``-n 0`` for
# a serial run when debugging a single test.
pytest-xdist>=3,<4
